// To do: list all users (map through and include key, username, image, about )
import React, { useState } from "react";

const Home = ({ home }) => {};
// lists all users to browse suggestions (GetAllUsers)
// click playlist to view userprofilepage
// user react strap slider?

export default Home;
